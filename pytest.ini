[pytest]
# Pin the rootdir to the repo root and put it on sys.path, so the tests can
# import the imu2text package no matter how pytest is invoked (bare `pytest`
# in CI, `python -m pytest`, or `pytest path/to/tests` from another
# directory) without the package having to be pip-installed first.
pythonpath = .
testpaths = tests
