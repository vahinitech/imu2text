# Core scientific stack
numpy==1.26.4
pandas==2.2.3
scipy==1.11.4
scikit-learn==1.5.2        # >=1.5.0 fixes CVE-2024-5206 (sensitive-data leakage in TfidfVectorizer)

# Deep learning frameworks
tensorflow==2.15.1         # Keras 2 API used by cnn_gnn.py / onhw_models.py; ships macOS arm64 wheels
                           # (the old tensorflow-macos / tensorflow-estimator pins are no longer needed)
torch==2.7.1               # >=2.6.0 fixes CVE-2025-32434 (RCE via torch.load even with weights_only=True);
                           # 2.7.1 also resolves the 2025 memory-corruption / DoS advisories
                           # (unpack_sequence, pad_packed_sequence, torch.jit.script, lstm_cell, mkldnn DoS, ...)
torchvision==0.22.1        # release matched to torch 2.7.1
torch-geometric==2.6.1     # GCNConv layers used in cnn_gnn.py

# Visualization & reporting
matplotlib==3.8.4          # 3.4.x has no wheels for Python >=3.10
tabulate==0.9.0            # result tables in cnn_gnn.py / onhw_models.py

# CUDA note: for GPU installs, use the matching wheel index instead of pinning "+cu" here, e.g.
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
# For CPU-only CI / laptops:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
